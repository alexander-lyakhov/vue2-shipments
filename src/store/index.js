import Vue from 'vue'
import Vuex from 'vuex'

import shipments from './imports.json'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    shipments
  },

  mutations: {
  },

  actions: {
  },

  modules: {
  }
})
